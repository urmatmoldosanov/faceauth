# FaceAuth

This repository can be collected into a single local directory with:

```bash
./scripts/collect_project.sh
```

By default, the command creates `collected_project/` at the repository root. The generated directory is ignored by Git so it can be recreated at any time.
